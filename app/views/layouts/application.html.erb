<!DOCTYPE html>
<html>
  <head>
    <script src="https://use.fontawesome.com/releases/v6.3.0/js/all.js" crossorigin="anonymous"></script>
    <title>PetalPush</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= include_gon %>
  </head>

  <body>
    <header class ="header">
      <div class ="header-icon">
      <% if user_signed_in? %>
        <%= link_to '<i class="fa-solid fa-house" style="color: #FFA6A6;"></i>'.html_safe, user_path(current_user.id) %>
        <%= link_to '<i class="fa-solid fa-right-from-bracket" style="color: #FFA6A6;"></i>'.html_safe, destroy_user_session_path, method: :delete %>
      <% else %>
        <%= link_to '<i class="fa-solid fa-right-to-bracket" style="color: #ffa6a6;"></i>'.html_safe, new_user_session_path %>
        <%= link_to '<i class="fa-solid fa-user-plus" style="color: #ffa6a6;"></i>'.html_safe, new_user_registration_path %>
      <% end %>
      

      <% if notice %>
        <p class="alert alert-notice"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-error"><%= alert %></p>
      <% end %>
    </header>

    <div class="content">
    <%= yield %>
    </div>

    <footer class ="footer">
      <p><a href="#"</a>PetalPush</p>
    </footer>
  </body>
</html>
